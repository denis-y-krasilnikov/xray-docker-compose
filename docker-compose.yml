---
version: "3"

services:
  3x-ui:
    image: ghcr.io/mhsanaei/3x-ui:v2.8.11
    container_name: 3x-ui
    hostname: yourhostnam
    environment:
      XRAY_VMESS_AEAD_FORCED: "false"
    tty: true
    network_mode: host
    restart: unless-stopped
